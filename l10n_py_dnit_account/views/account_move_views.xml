<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_move_form_l10n_py" model="ir.ui.view">
        <field name="name">account.move.form.l10n.py</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <field name="l10n_latam_document_type_id" position="after">
                <field name="l10n_py_authorization_id"
                       invisible="not l10n_latam_use_documents or move_type not in ('out_invoice', 'out_refund')"
                       required="l10n_latam_use_documents and move_type in ('out_invoice', 'out_refund') and country_code == 'PY'"/>
                <field name="l10n_py_full_number" invisible="not l10n_py_full_number"/>
            </field>
        </field>
    </record>

</odoo>
