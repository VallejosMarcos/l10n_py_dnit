# Instalación

1. Copiar el módulo a una ruta incluida en el `addons_path` de Odoo.
2. En **Aplicaciones**, actualizar la lista e instalar
   **Paraguay - Plan de Cuentas DNIT**.

Requisitos:

- Odoo 19.0
- Módulo `account` (núcleo de Odoo), que se instala automáticamente como
  dependencia.
